// API Configuration
// Use empty string for relative URLs to leverage Next.js proxy and avoid CORS issues
// The Next.js proxy (configured in next.config.ts) will route /api/* to http://localhost:8000/api/*
export const API_BASE_URL = '';
export const API_BASE = '';
